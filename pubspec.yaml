name: screen_time_pro
description: Screen time tracker
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  usage_stats: ^1.2.0
  device_apps: ^2.2.0
  fl_chart: ^0.69.1
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
